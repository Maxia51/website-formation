{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
    <h2 class="text-2xl font-bold">Last Formations</h2>
    <hr/>

    {% for formation in formations %}

        <li><a href="{{ path('formation_detail', { id : formation.id}) }}">{{ formation.title }}</a></li>

    {% endfor %}
</div>
{% endblock %}
