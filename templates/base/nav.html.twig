{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('home') }}">MyFormation</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('admin.formation.index') }}">Formations</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('formation') }}">Formations</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('admin.lesson.index') }}">Lessons</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('lesson') }}">Lessons</a>
                    {% endif %}
                </li>

                {% if app.user %}
                    {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}">Logout</a>
                    </li>
                {% endif %}

            </ul>
        </div>
    </nav>
{% endblock %}